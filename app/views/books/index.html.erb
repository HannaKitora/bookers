<h1>Books</h1>

<% @book.each do |list| %>

  <p>Title</p>
  <span><%= book.title %></span>
  <p>Body</p>
  <span><%= book.body %></span>


<% end %>



<h3>New book</h3>

<%= form_with model: @book, url: '/show', method: :post do |f| %>
  <h5>Title</h5>
  <%= f.text_field :title %>
  
  <h5>Body</h5>
  <%= f.text_area :body %>
  
  <br>
  <%= f.submit 'Create Book' %>
  
<% end %>