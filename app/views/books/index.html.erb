<h1>Books</h1>

<table class="is_border">

  <div class="book-box">
    <thead class="is_border">
      <tr>
        <th><strange>Title</strange></th>
        <th><strange>Body</strange></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody class="is_border">
      <% @books.each do |book| %>
      <tr>
        <td>
          <!--<span class="book-title">-->
          <%=book.title %>
          <!--</span>-->
        </td>
        <td>
          <!--<span class="book-body">-->
          <%=book.body %>
          <!--</span>-->
        </td>
        <td>
           <%= link_to "Show", book_path(book.id) %>
        </td>
        <td>
          <%= link_to 'Edit', edit_book_path(book.id) %>
        </td>
        <td>
          <%= link_to 'Destroy', book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </div>
</table>
  


<h2>New Book</h2>

<!--<%= form_with model: @book do |f| %>-->
<!--  <div class="field">-->
<!--    <%= f.label :title %>-->
<!--    <%= f.text_field :title %>-->
<!--  </div>-->
  
<!--  <div class="field">-->
<!--    <%= f.label :body %>-->
<!--    <%= f.text_area :body %>-->
<!--  </div>-->
  
<!--  <div class="actions">-->
<!--    <%= f.submit 'Create Book' %>-->
<!--  </div>-->
<!--<% end %>-->

<div class="form">
<%= form_with model: @book do |f| %>
  <div>Title</div>
  <%= f.text_field :title %>
  <div>Body</div>
  <%= f.text_area :body %>
  <br>
  <%= f.submit 'Create Book' %>
  
<% end %>
</div>